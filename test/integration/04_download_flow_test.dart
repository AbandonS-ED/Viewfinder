import 'package:flutter_test/flutter_test.dart';

/// T4 验证 GalleryPage 选 2 张 → 下载 → 写相册
/// 当前 downloadManagerProvider.downloadSelected 是 unit-tested
/// (test/features/downloads/download_manager_view_model_test.dart)
/// 完整 E2E flow 待 Mac + iPhone 验证

void main() {
  test('T4 placeholder', () {
    expect(1 + 1, 2);
  });
}